{% extends 'baseuser.html.twig' %}

{% block title %}Liste des Laveries{% endblock %}

{% block body %}
<div class="contact-header" style="background-image: url({{ asset('images/laverie.jpg') }});">
    <h1>NOS LAVERIES</h1>
    <p>home > laverie</p>
</div>


<div class="row">
    {% for laverie in laveries %}
        <div class="col-md-4">
            <div class="card mb-4">
               
                <div class="card-body">
                    <h5 class="card-title">{{ laverie.nomLaverie }}</h5>
                     <img src="{{ asset('images/wash-service.png') }}" class="iimg-top" style=" width: 120px;  
    height: auto; margin: 0 auto; " alt="Image de la laverie">
                    <p class="card-text">Description de la laverie {{ laverie.nomLaverie }} dans le bloc .</p>
                    <a href="{{ path('laverie_detail', { id: laverie.id }) }}" class="btn btn-primary">Voir les détails</a>
                </div>
            </div>
        </div>
    {% endfor %}
</div>

{% endblock %}


