{% extends 'basedash.html.twig' %}

{% block title %}Machine{% endblock %}

{% block body %}

    <h1>Machine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ machine.id }}</td>
            </tr>
 
            <tr>
                <th>StatutMachine</th>
                <td>{{ machine.statutMachine }}</td>
            </tr>
            <tr>
                <th>TypeMachine</th>
                <td>{{ machine.typeMachine }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_machine_index') }}" class="btn btn-primary">Revenir à la liste</a><br>

    <a href="{{ path('app_machine_edit', {'id': machine.id}) }}" class="btn btn-primary">Modifier</a><br>

    {{ include('machine/_delete_form.html.twig') }}
{% endblock %}
