{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié{% endblock %}

{% block body %}
<div class="contact-header" style="background-image: url({{asset('images/fond.jpg')}});">
    <h1>Mot de Passe Oublié</h1>
    <p>CONNECTEZ VOUS> mot de passe oublié</p>
</div>
<div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh;">
    <div class="card shadow-lg p-4" style="max-width: 400px; width: 100%; border-radius: 8px;">
        <div class="text-center mb-3">
            <img src="{{ asset('images/logoedutine.png') }}" alt="Logo" style="max-width: 100px; /* Ajustez selon vos besoins */
    max-height: 50px;
    object-fit: contain;">
            <h5 class="mt-2">Mot de Passe Oublié? <span style="color: #6c63ff;">🔒</span></h5>
            <p class="text-muted">Entrez votre email pour créer un nouveau mot de passe</p>
        </div>
        <form action="{{ path('reset_password') }}" method="post">
            <div class="mb-3">
                <label for="email" class="form-label">Email</label>
                <input type="email" class="form-control" id="email" name="email" placeholder="Entrer votre email" required>
            </div>
            <button type="submit" class="btn btn-primary w-100" style="background-color: #6c63ff; border: none;">envoyer un lien </button>
        </form>
        <div class="text-center mt-3">
            <a href="{{ path('app_login') }}" class="text-muted">retour à la connexion</a>
        </div>
    </div>
</div>

{% endblock %}
