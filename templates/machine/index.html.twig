{% extends 'basedash.html.twig' %}

{% block title %}Machine index{% endblock %}

{% block body %}

<div class="contact-header" style="background-image: url({{asset('images/fond.jpg')}});">
    <h1>Contactez-nous</h1>
    <p>HOME > CONTACT</p>
</div>
    <h1>Machine index</h1>

    <table class="table">
    
        <thead>
            <tr>
                <th>Id</th>
                <th>StatutMachine</th>
                <th>TypeMachine</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for machine in machines %}
        <form action="{{path('Recherche',{'tt':machine.typeMachine})}}" method="">
            <tr>
                <td>{{ machine.id }}</td>
                <td>{{ machine.statutMachine }}</td>
                <td>{{ machine.typeMachine }}</td>
                <td>
                    <a href="{{ path('app_machine_show', {'id': machine.id}) }}" class="btn btn-primary">show</a>
                    <a href="{{ path('app_machine_edit', {'id': machine.id}) }}" class="btn btn-primary">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
<input type="text" placeholder="search" name="ss">
    <input type="submit" value="search">
</form>
</form>
        </tbody>
    </table>

    <a href="{{ path('app_machine_new') }}" class="btn btn-primary">Réclamer !</a>
  <br>
<a href="{{ path('pdf') }}" class="btn btn-primary">Exporter vers PDF</a>
<br> 
     <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jqueryui/1.12.1/jquery-ui.js"></script>
    <script src="parallax.min.js"></script>
    <script>
        // Initialisation du calendrier
        $(document).ready(function(){
            $("#datepicker").datepicker();
        });

        // Initialisation du parallax
        $('.parallax-window').parallax();
    </script>
{% endblock %}
