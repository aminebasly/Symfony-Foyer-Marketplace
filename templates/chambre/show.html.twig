{% extends 'basedash.html.twig' %}

{% block title %}Chambre{% endblock %}

{% block body %}
    <h1>Chambre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdChambre</th>
                <td>{{ chambre.idChambre }}</td>
            </tr>
            <tr>
                <th>NumChambre</th>
                <td>{{ chambre.numChambre }}</td>
            </tr>
            <tr>
                <th>Etage</th>
                <td>{{ chambre.etage }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ chambre.capacite }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ chambre.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chambre_index') }}">revenir à la liste</a>

    <a href="{{ path('app_chambre_edit', {'idChambre': chambre.idChambre}) }}">Modifier</a>

    {{ include('chambre/_delete_form.html.twig') }}
{% endblock %}
