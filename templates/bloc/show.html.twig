{% extends 'basedash.html.twig' %}

{% block title %}Bloc{% endblock %}

{% block body %}
    <h1>Bloc</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdBloc</th>
                <td>{{ bloc.idBloc }}</td>
            </tr>
            <tr>
                <th>NomBloc</th>
                <td>{{ bloc.nomBloc }}</td>
            </tr>
            <tr>
                <th>CapaciteBloc</th>
                <td>{{ bloc.capaciteBloc }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bloc_index') }}">back to list</a>

    <a href="{{ path('app_bloc_edit', {'idBloc': bloc.idBloc}) }}">edit</a>

    {{ include('bloc/_delete_form.html.twig') }}
{% endblock %}
