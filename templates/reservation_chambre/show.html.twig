{% extends 'base.html.twig' %}

{% block title %}ReservationChambre{% endblock %}

{% block body %}
    <h1>ReservationChambre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdReservationChambre</th>
                <td>{{ reservation_chambre.idReservationChambre }}</td>
            </tr>
            <tr>
                <th>AnneeUniversitaire</th>
                <td>{{ reservation_chambre.anneeUniversitaire ? reservation_chambre.anneeUniversitaire|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>EstValide</th>
                <td>{{ reservation_chambre.estValide ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_chambre_index') }}">back to list</a>

    <a href="{{ path('app_reservation_chambre_edit', {'idReservationChambre': reservation_chambre.idReservationChambre}) }}">edit</a>

    {{ include('reservation_chambre/_delete_form.html.twig') }}
{% endblock %}
